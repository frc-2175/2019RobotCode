plugins {
    id 'java'
}

sourceSets {
    main {
        java {
            srcDir '.'
        }
    }
}

jar {
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    manifest {
        attributes 'Main-Class': 'Riolog'
    }
}
